

## Replace Bhakti Marga Logo

Replace the current Bhakti Marga logo in the OrgTicker with the new uploaded image.

### Steps

1. Copy `user-uploads://BM-Logo31_bw-3.png` to `src/assets/org-bhaktimarga.png` (overwrite existing)
2. No code changes needed — the import in `OrgTicker.tsx` already references `@/assets/org-bhaktimarga.png`

